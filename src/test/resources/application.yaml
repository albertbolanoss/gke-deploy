server:
  port: 9081
spring:
  application:
    name: LABS_GKE_DEPLOY
  kafka:
    bootstrap-servers: localhost:9092
    streams:
      application-id: ${spring.application.name}
  data:
    redis:
      host: localhost
      port: 6379
      password: password