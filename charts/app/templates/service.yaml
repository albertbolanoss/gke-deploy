apiVersion: v1
kind: Service
metadata:
  name: {{ include "labs-helmchart-base.buildName" (list . "svc") }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "labs-helmchart-base.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "labs-helmchart-base.selector-labels" . | nindent 4 }}
